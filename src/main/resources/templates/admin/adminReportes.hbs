{{#partial "contenido"}}
    <div class="m-5">
        <h2 class="fw-bold">Reportes</h2>
        <p>
            Los reportes se generan semanalmente para hacer un seguimiento detallado del trabajo de todos los involucrados en la causa.
        </p>
        {{#each reportes}}
            <div class="report-card mt-5">
                <div class="row p-3">
                    <div class="col-12 col-md-6 d-flex justify-content-center justify-content-md-start mb-2 mb-md-0">
                        <h3 class="fw-bold text-center text-md-start">{{this.fecha}}</h3>
                    </div>
                    <div class="col-12 col-md-6 d-flex justify-content-center justify-content-md-end">
                        <div class="dropdown">
                            <button class="btn-dropdown dropdown-toggle" type="button" id="dropdownMenuButton-two" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                                Ver reporte
                            </button>
                            <div class="dropdown-menu" aria-labelledby="dropdownMenuButton-two">
                                <a class="dropdown-item" onclick="mostrarPDF('{{this.rutaFallas}}')" data-reporte="fallas">Cantidad de fallas por Heladera</a>
                                <a class="dropdown-item" onclick="mostrarPDF('{{this.rutaViandasRetiradas}}')" data-reporte="viandasRetiradas">Cantidad de Viandas Retiradas/Colocadas por Heladera</a>
                                <a class="dropdown-item" onclick="mostrarPDF('{{this.rutaViandasDonadas}}')" data-reporte="viandasDonadas">Cantidad de Viandas Donadas por colaborador</a>
                            </div>
                        </div>
                    </div>
                </div>
                <div id="pdfContainer" class="p-3" style="display: none; width: 100%; height: 80vh;">
                    <div id="pdfViewer" class="rounded-10" style="width: 100%; height: 100%; border-radius: 5px; overflow: hidden;"></div>
                </div>
            </div>
        {{/each}}
    </div>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/pdfobject/2.2.6/pdfobject.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/js/bootstrap.bundle.min.js"></script>
    <script>
        function mostrarPDF(ruta) {
            document.getElementById('pdfContainer').style.display = 'block';
            PDFObject.embed(ruta, "#pdfViewer", {
                height: "100%",
                width: "100%"
            });
        }
    </script>
{{/partial}}

{{> templates/base titulo="Reportes" mostrarAdmin=true mostrarIrAtras=true paginaAtras="/" mostrarSeccion=true seccion="Reportes" }}
